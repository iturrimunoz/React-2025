import { useState } from "react";

const Default = () => {

    const [count, setCount] = useState(0)

    return (
    <div>
     
      
      </div>
    );
};

export default Default;
// Export default de classes from React